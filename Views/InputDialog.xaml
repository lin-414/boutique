<Window x:Class="Boutique.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:res="clr-namespace:Boutique.Resources"
        Title="{lex:Loc Key={x:Static res:LocalizationKeys+View.InputTitle}}"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow"
        BorderThickness="0"
        Background="{StaticResource Brush.CardBackground}">

  <Grid Margin="20">
    <Grid.LayoutTransform>
      <ScaleTransform x:Name="RootScaleTransform" ScaleX="1" ScaleY="1" />
    </Grid.LayoutTransform>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               x:Name="PromptText"
               TextWrapping="Wrap"
               FontSize="14"
               Margin="0,0,0,12"
               Foreground="{StaticResource Brush.TextPrimary}" />

    <TextBox Grid.Row="1"
             x:Name="InputTextBox"
             FontSize="14"
             Padding="8,6"
             KeyDown="InputTextBox_KeyDown" />

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+Buttons.Cancel}}"
              Click="CancelButton_Click"
              IsCancel="True"
              MinWidth="80"
              Margin="0,0,10,0"
              Padding="12,6" />
      <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+View.Ok}}"
              Click="OkButton_Click"
              IsDefault="True"
              MinWidth="80"
              Padding="12,6" />
    </StackPanel>
  </Grid>
</Window>
