<UserControl x:Class="Boutique.Views.DistributionReportCardTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Boutique.ViewModels"
             xmlns:utilities="clr-namespace:Boutique.Utilities"
             xmlns:themes="clr-namespace:Boutique.Themes"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:res="clr-namespace:Boutique.Resources"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DistributionReportCardTabViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="700"
             d:DesignWidth="900">
  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVis" />

    <Style x:Key="GradeLetter" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="{StaticResource Font.Handwriting}" />
      <Setter Property="FontSize" Value="{DynamicResource FontSize.Display}" />
      <Setter Property="FontWeight" Value="Normal" />
      <Setter Property="HorizontalAlignment" Value="Center" />
      <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextSecondary}}" />
      <Style.Triggers>
        <DataTrigger Binding="{Binding}" Value="A">
          <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushSuccess}}" />
        </DataTrigger>
        <DataTrigger Binding="{Binding}" Value="B">
          <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushInfo}}" />
        </DataTrigger>
        <DataTrigger Binding="{Binding}" Value="C">
          <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushConflict}}" />
        </DataTrigger>
        <DataTrigger Binding="{Binding}" Value="D">
          <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushWarning}}" />
        </DataTrigger>
        <DataTrigger Binding="{Binding}" Value="F">
          <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushWarning}}" />
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="GradePercent" TargetType="TextBlock">
      <Setter Property="FontSize" Value="{DynamicResource FontSize.Medium}" />
      <Setter Property="HorizontalAlignment" Value="Center" />
      <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextSecondary}}" />
    </Style>

    <Style x:Key="GroupHeader" TargetType="TextBlock">
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Margin" Value="0,6,0,2" />
      <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextPrimary}}" />
    </Style>

    <Style x:Key="GroupItems" TargetType="TextBlock">
      <Setter Property="Margin" Value="12,0,0,4" />
      <Setter Property="TextWrapping" Value="Wrap" />
      <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextSecondary}}" />
    </Style>

    <Style x:Key="RankingColumnHeader" TargetType="TextBlock">
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="FontSize" Value="{DynamicResource FontSize.StatCaption}" />
      <Setter Property="Margin" Value="0,0,0,6" />
      <Setter Property="Foreground" Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextPrimary}}" />
    </Style>

    <DataTemplate x:Key="RankingRowTemplate">
      <Grid Margin="0,1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0"
                   Text="{Binding Label}"
                   FontSize="{DynamicResource FontSize.Small}"
                   TextTrimming="CharacterEllipsis"
                   Foreground="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextSecondary}}" />
        <TextBlock Grid.Column="1"
                   FontSize="{DynamicResource FontSize.Small}"
                   Margin="8,0,0,0"
                   Foreground="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextDisabled}}">
          <TextBlock.Text>
            <MultiBinding StringFormat="{}{0}/{1}">
              <Binding Path="UncoveredCount" />
              <Binding Path="TotalCount" />
            </MultiBinding>
          </TextBlock.Text>
        </TextBlock>
      </Grid>
    </DataTemplate>

    <Style x:Key="ClickableRankingButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="2,1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    Padding="{TemplateBinding Padding}"
                    CornerRadius="3">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background"
                        Value="{DynamicResource {x:Static utilities:ResourceKeys.BrushBorderLight}}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Row 0: Grade Cards -->
    <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,8">

      <!-- Overall -->
      <Border Style="{DynamicResource {x:Static themes:ResourceKeys.StatCardStyleKey}}">
        <StackPanel>
          <TextBlock Text="{Binding}"
                     Style="{StaticResource GradeLetter}"
                     DataContext="{Binding OverallGrade}" />
          <TextBlock Style="{StaticResource GradePercent}"
                     Text="{Binding OverallPercent, StringFormat='{}{0:P0}'}" />
          <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.Overall}}" Style="{DynamicResource {x:Static themes:ResourceKeys.StatCardCaptionStyleKey}}" />
        </StackPanel>
      </Border>

      <!-- NPC Coverage -->
      <Border Style="{DynamicResource {x:Static themes:ResourceKeys.StatCardStyleKey}}">
        <StackPanel>
          <TextBlock Text="{Binding}"
                     Style="{StaticResource GradeLetter}"
                     DataContext="{Binding NpcCoverageGrade}" />
          <TextBlock Style="{StaticResource GradePercent}"
                     Text="{Binding NpcCoveragePercent, StringFormat='{}{0:P0}'}" />
          <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.NpcCoverage}}" Style="{DynamicResource {x:Static themes:ResourceKeys.StatCardCaptionStyleKey}}" />
        </StackPanel>
      </Border>

      <!-- Mod Utilization -->
      <Border Style="{DynamicResource {x:Static themes:ResourceKeys.StatCardStyleKey}}">
        <StackPanel>
          <TextBlock Text="{Binding}"
                     Style="{StaticResource GradeLetter}"
                     DataContext="{Binding ModUtilizationGrade}" />
          <TextBlock>
            <TextBlock.Style>
              <Style TargetType="TextBlock" BasedOn="{StaticResource GradePercent}">
                <Setter Property="Text" Value="{Binding ModUtilizationPercent, StringFormat='{}{0:P0}'}" />
                <Style.Triggers>
                  <DataTrigger Binding="{Binding HasModOutfits}" Value="False">
                    <Setter Property="Text" Value="{lex:Loc Key={x:Static res:LocalizationKeys+View.NotApplicable}}" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </TextBlock.Style>
          </TextBlock>
          <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.ModUtilization}}" Style="{DynamicResource {x:Static themes:ResourceKeys.StatCardCaptionStyleKey}}" />
        </StackPanel>
      </Border>

      <!-- Variety -->
      <Border Style="{DynamicResource {x:Static themes:ResourceKeys.StatCardStyleKey}}">
        <StackPanel>
          <TextBlock Text="{Binding}"
                     Style="{StaticResource GradeLetter}"
                     DataContext="{Binding VarietyGrade}" />
          <TextBlock Style="{StaticResource GradePercent}"
                     Text="{Binding VarietyPercent, StringFormat='{}{0:P0}'}" />
          <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.OutfitVariety}}" Style="{DynamicResource {x:Static themes:ResourceKeys.StatCardCaptionStyleKey}}" />
        </StackPanel>
      </Border>
    </UniformGrid>

    <!-- Row 1: Calculate Button -->
    <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,4,0,8">
      <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+View.CalculateGrade}}"
              Command="{Binding CalculateCommand}"
              Style="{DynamicResource {x:Static themes:ResourceKeys.PrimaryButtonStyleKey}}"
              Padding="24,8"
              FontSize="{DynamicResource FontSize.Medium}" />
      <TextBlock Margin="0,6,0,0"
                 HorizontalAlignment="Center"
                 Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}"
                 Foreground="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextSecondary}}">
        <TextBlock.Text>
          <MultiBinding Converter="{StaticResource FormatConverter}">
            <lex:Loc Key="{x:Static res:LocalizationKeys+View.CoverageSummaryFormat}" />
            <Binding Path="CoveredNpcCount" />
            <Binding Path="EligibleNpcCount" />
            <Binding Path="UsedModOutfitCount" />
            <Binding Path="ModOutfitCount" />
            <Binding Path="UniqueOutfitCount" />
          </MultiBinding>
        </TextBlock.Text>
      </TextBlock>
    </StackPanel>

    <!-- Row 2: Empty state -->
    <TextBlock Grid.Row="2"
               HorizontalAlignment="Center"
               Margin="0,16,0,8"
               FontStyle="Italic"
               Foreground="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextDisabled}}"
               Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.ClickCalculateHint}}">
      <TextBlock.Style>
        <Style TargetType="TextBlock">
          <Setter Property="Visibility" Value="Collapsed" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding HasResults}" Value="False">
              <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </TextBlock.Style>
    </TextBlock>

    <!-- Row 3: Suggestion Panels -->
    <ScrollViewer Grid.Row="3"
                  VerticalScrollBarVisibility="Auto"
                  Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
      <StackPanel Margin="4,0">

        <!-- Uncovered NPCs — Attribute Rankings -->
        <Grid Margin="0,0,0,8">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0" Margin="0,0,8,0">
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+Tabs.Factions}}" Style="{StaticResource RankingColumnHeader}" />
            <ItemsControl ItemsSource="{Binding UncoveredByFaction}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Button Style="{StaticResource ClickableRankingButton}"
                          Command="{Binding DataContext.NavigateToFactionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          CommandParameter="{Binding}">
                    <ContentControl Content="{Binding}"
                                    ContentTemplate="{StaticResource RankingRowTemplate}" />
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>

          <StackPanel Grid.Column="1" Margin="4,0,8,0">
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+Tabs.Classes}}" Style="{StaticResource RankingColumnHeader}" />
            <ItemsControl ItemsSource="{Binding UncoveredByClass}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Button Style="{StaticResource ClickableRankingButton}"
                          Command="{Binding DataContext.NavigateToClassCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          CommandParameter="{Binding}">
                    <ContentControl Content="{Binding}"
                                    ContentTemplate="{StaticResource RankingRowTemplate}" />
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>

          <StackPanel Grid.Column="2" Margin="4,0,8,0">
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+Tabs.Races}}" Style="{StaticResource RankingColumnHeader}" />
            <ItemsControl ItemsSource="{Binding UncoveredByRace}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Button Style="{StaticResource ClickableRankingButton}"
                          Command="{Binding DataContext.NavigateToRaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          CommandParameter="{Binding}">
                    <ContentControl Content="{Binding}"
                                    ContentTemplate="{StaticResource RankingRowTemplate}" />
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>

          <StackPanel Grid.Column="3" Margin="4,0,0,0">
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.SourceMods}}" Style="{StaticResource RankingColumnHeader}" />
            <ItemsControl ItemsSource="{Binding UncoveredByMod}"
                          ItemTemplate="{StaticResource RankingRowTemplate}" />
          </StackPanel>
        </Grid>

        <!-- Unused Mod Outfits -->
        <Expander IsExpanded="True">
          <Expander.Header>
            <TextBlock FontWeight="SemiBold">
              <TextBlock.Text>
                <MultiBinding Converter="{StaticResource FormatConverter}">
                  <lex:Loc Key="{x:Static res:LocalizationKeys+View.UnusedOutfitsFormat}" />
                  <Binding Path="UnusedOutfits.Count" />
                </MultiBinding>
              </TextBlock.Text>
            </TextBlock>
          </Expander.Header>
          <DataGrid ItemsSource="{Binding UnusedOutfits}"
                    Margin="0,4,0,0" MaxHeight="360"
                    AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                    IsReadOnly="True" CanUserSortColumns="True" SelectionMode="Single" SelectionUnit="FullRow"
                    GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                    EnableRowVirtualization="True" EnableColumnVirtualization="True"
                    RowHeight="32">
            <DataGrid.Columns>
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.EditorID}}" Binding="{Binding EditorID}" Width="2*" CanUserSort="True" />
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+View.Plugin}}" Binding="{Binding ModDisplayName}" Width="1.5*" CanUserSort="True"
                                  ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridSecondaryColumnStyleKey}}" />
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.FormKey}}" Binding="{Binding FormKeyString}" Width="1.5*" CanUserSort="True"
                                  ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridFormKeyColumnStyleKey}}" />
              <DataGridTemplateColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.PreviewCol}}" Width="Auto">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+Buttons.ShowPreview}}"
                            Command="{Binding DataContext.PreviewOutfitCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}" Padding="8,2" Margin="4,0" HorizontalAlignment="Center" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
            </DataGrid.Columns>
          </DataGrid>
        </Expander>

        <!-- Armors Not In Any Outfit -->
        <Expander IsExpanded="False" Margin="0,8,0,0">
          <Expander.Header>
            <TextBlock FontWeight="SemiBold">
              <TextBlock.Text>
                <MultiBinding Converter="{StaticResource FormatConverter}">
                  <lex:Loc Key="{x:Static res:LocalizationKeys+View.UnusedArmorsFormat}" />
                  <Binding Path="UnusedArmors.Count" />
                </MultiBinding>
              </TextBlock.Text>
            </TextBlock>
          </Expander.Header>
          <StackPanel Margin="0,4,0,0">
            <TextBlock Style="{StaticResource GroupItems}"
                       FontStyle="Italic"
                       Margin="0,0,0,6"
                       Foreground="{DynamicResource {x:Static utilities:ResourceKeys.BrushTextDisabled}}"
                       Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.UnusedArmorsHint}}" />
            <DataGrid ItemsSource="{Binding UnusedArmors}"
                      MaxHeight="360"
                      AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                      IsReadOnly="True" CanUserSortColumns="True" SelectionMode="Single" SelectionUnit="FullRow"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True"
                      RowHeight="32">
              <DataGrid.Columns>
                <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.Name}}" Binding="{Binding DisplayName}" Width="2*" CanUserSort="True" />
                <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.Slots}}" Binding="{Binding SlotSummary}" Width="1.5*" CanUserSort="True"
                                    ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridSecondaryColumnStyleKey}}" />
                <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+View.Plugin}}" Binding="{Binding ModDisplayName}" Width="1.5*" CanUserSort="True"
                                    ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridSecondaryColumnStyleKey}}" />
                <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.FormID}}" Binding="{Binding FormIdDisplay}" Width="1*" CanUserSort="True"
                                    ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridFormKeyColumnStyleKey}}" />
                <DataGridTemplateColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.PreviewCol}}" Width="Auto">
                  <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                      <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+Buttons.ShowPreview}}"
                              Command="{Binding DataContext.PreviewArmorCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding}" Padding="8,2" Margin="4,0" HorizontalAlignment="Center" />
                    </DataTemplate>
                  </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
              </DataGrid.Columns>
            </DataGrid>
          </StackPanel>
        </Expander>

      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>
