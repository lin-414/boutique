<UserControl x:Class="Boutique.Views.OutfitCreatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Boutique.ViewModels"
             xmlns:themes="clr-namespace:Boutique.Themes"
             xmlns:utilities="clr-namespace:Boutique.Utilities"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:res="clr-namespace:Boutique.Resources"
             xmlns:views="clr-namespace:Boutique.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:OutfitCreatorViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="800"
             d:DesignWidth="1400">
  <UserControl.Resources>
    <views:OutfitQueueItemTemplateSelector x:Key="QueueItemTemplateSelector">
      <views:OutfitQueueItemTemplateSelector.DraftTemplate>
        <DataTemplate DataType="{x:Type vm:OutfitDraftViewModel}">
          <Grid Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisConverter}}"
                AllowDrop="True"
                DragEnter="QueueItem_OnDragEnter"
                DragLeave="QueueItem_OnDragLeave"
                DragOver="QueueItem_OnDragOver"
                Drop="QueueItem_OnDrop">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border x:Name="DropIndicatorTop" Grid.Row="0" Height="2" Background="{DynamicResource Brush.Info}"
                    Margin="0,0,0,2" Visibility="Collapsed" />

            <Grid Grid.Row="1" Margin="0,0,0,0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <Rectangle Grid.Column="0" Width="3" Fill="{DynamicResource Brush.Border}"
                         Margin="4,-6,10,-6" />
              <Border Grid.Column="1" Padding="6" Margin="0,0,0,6" BorderBrush="{DynamicResource Brush.Border}"
                      BorderThickness="1" CornerRadius="3" Opacity="0.6">
                <StackPanel>
                  <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="⠿" FontSize="{DynamicResource FontSize.Medium}" Margin="0,0,6,0"
                               Foreground="{DynamicResource Brush.TextSecondary}"
                               VerticalAlignment="Top" Cursor="SizeAll"
                               ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+View.DragToReorder}}"
                               PreviewMouseLeftButtonDown="DragHandle_PreviewMouseLeftButtonDown"
                               PreviewMouseMove="DragHandle_PreviewMouseMove"
                               PreviewMouseLeftButtonUp="DragHandle_PreviewMouseLeftButtonUp" />

                    <ToggleButton Grid.Column="1"
                                  IsChecked="{Binding IsExpanded}"
                                  Width="20" Height="20"
                                  Margin="0,0,6,0"
                                  VerticalAlignment="Top"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  Cursor="Hand">
                      <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                          <Setter Property="Template">
                            <Setter.Value>
                              <ControlTemplate TargetType="ToggleButton">
                                <Border Background="{TemplateBinding Background}">
                                  <TextBlock x:Name="Arrow" Text="▶" FontSize="{DynamicResource FontSize.Small}"
                                             HorizontalAlignment="Center" VerticalAlignment="Center"
                                             Foreground="{DynamicResource Brush.TextSecondary}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                  <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Arrow" Property="Text" Value="▼" />
                                  </Trigger>
                                  <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Arrow" Property="Foreground"
                                            Value="{DynamicResource Brush.TextPrimary}" />
                                  </Trigger>
                                </ControlTemplate.Triggers>
                              </ControlTemplate>
                            </Setter.Value>
                          </Setter>
                        </Style>
                      </ToggleButton.Style>
                    </ToggleButton>

                    <StackPanel Grid.Column="2" Orientation="Vertical">
                      <StackPanel Orientation="Horizontal">
                        <TextBox Width="180"
                                 Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PreviewTextInput="OutfitNameTextBox_OnPreviewTextInput"
                                 DataObject.Pasting="OutfitNameTextBox_OnPasting"
                                 MaxLength="40" HorizontalAlignment="Left"
                                 IsReadOnly="{Binding IsOverride}">
                          <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                              <Style.Triggers>
                                <DataTrigger
                                  Binding="{Binding Path=(views:OutfitCreatorView.IsDragging), RelativeSource={RelativeSource AncestorType=views:OutfitCreatorView}}"
                                  Value="True">
                                  <Setter Property="IsHitTestVisible" Value="False" />
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </TextBox.Style>
                        </TextBox>
                        <TextBlock
                                   Margin="8,0,0,0" FontSize="{DynamicResource FontSize.Small}" Foreground="{DynamicResource Brush.TextDisabled}"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding IsExpanded, Converter={StaticResource InverseBoolToVisConverter}}">
                          <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource FormatConverter}" ConverterParameter="View_PiecesFormat">
                  <Binding Path="PieceCount" />
                </MultiBinding>
                          </TextBlock.Text>
                        </TextBlock>
                      </StackPanel>
                      <TextBlock Text="{Binding OverrideDisplayText, Mode=OneWay}"
                                 Margin="0,2,0,0" FontSize="{DynamicResource FontSize.Small}" Foreground="{DynamicResource Brush.Conflict}" FontWeight="SemiBold"
                                 Visibility="{Binding IsOverride, Converter={StaticResource BoolToVisConverter}}" />
                    </StackPanel>
                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Top">
                      <Button Content="👁" Command="{Binding PreviewCommand}"
                              Style="{DynamicResource {x:Static themes:ResourceKeys.IconButtonStyleKey}}"
                              Width="28" Height="28" Margin="0,0,4,0" FontSize="{DynamicResource FontSize.Medium}"
                              ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.PreviewOutfit}}" />
                      <Button Content="⎘" Command="{Binding DuplicateCommand}"
                              Style="{DynamicResource {x:Static themes:ResourceKeys.IconButtonStyleKey}}"
                              Width="28" Height="28" Margin="0,0,4,0" FontSize="{DynamicResource FontSize.Medium}"
                              ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.DuplicateOutfit}}" />
                      <Button Command="{Binding RemoveSelfCommand}"
                              Style="{DynamicResource {x:Static themes:ResourceKeys.InlineRemoveButtonStyleKey}}"
                              Width="28" Height="28" FontSize="{DynamicResource FontSize.Medium}"
                              ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.RemoveOutfit}}" />
                    </StackPanel>
                  </Grid>

                  <StackPanel Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisConverter}}">
                    <ItemsControl ItemsSource="{Binding Pieces}" Margin="26,0,0,0">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <Grid Margin="0,1,0,1">
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="*" />
                              <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding SlotSummary}"
                                       VerticalAlignment="Center" FontSize="{DynamicResource FontSize.Small}">
                              <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                  <Setter Property="Foreground" Value="{DynamicResource Brush.TextDisabled}" />
                                  <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConflicting}" Value="True">
                                      <Setter Property="Foreground" Value="{DynamicResource Brush.Warning}" />
                                      <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                  </Style.Triggers>
                                </Style>
                              </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}"
                                       VerticalAlignment="Center" TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis" FontSize="{DynamicResource FontSize.Small}">
                              <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                  <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConflicting}" Value="True">
                                      <Setter Property="Foreground" Value="{DynamicResource Brush.Warning}" />
                                    </DataTrigger>
                                  </Style.Triggers>
                                </Style>
                              </TextBlock.Style>
                            </TextBlock>
                            <Button Grid.Column="2"
                                    Command="{Binding DataContext.RemovePieceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{DynamicResource {x:Static themes:ResourceKeys.InlineRemoveButtonStyleKey}}"
                                    Margin="4,0,0,0" FontSize="{DynamicResource FontSize.Small}"
                                    ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.RemovePiece}}" />
                          </Grid>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.NoPiecesSelected}}"
                               Style="{DynamicResource {x:Static themes:ResourceKeys.EmptyStateTextStyleKey}}"
                               HorizontalAlignment="Left" FontSize="{DynamicResource FontSize.Small}" Margin="26,2,0,0"
                               Visibility="{Binding HasPieces, Converter={StaticResource InverseBoolToVisConverter}}" />

                    <ItemsControl ItemsSource="{Binding LeveledLists}" Margin="26,2,0,0"
                                  Visibility="{Binding HasLeveledLists, Converter={StaticResource BoolToVisConverter}}">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:OutfitLeveledListRefViewModel}">
                          <Grid Margin="0,1,0,1">
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="*" />
                              <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.ListBadge}}"
                                       VerticalAlignment="Center"
                                       FontSize="{DynamicResource FontSize.Small}"
                                       Foreground="{DynamicResource Brush.Type.Outfit}" />
                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis" FontSize="{DynamicResource FontSize.Small}"
                                       FontFamily="{DynamicResource Font.Mono}" />
                            <Button Grid.Column="2" Command="{Binding RemoveCommand}"
                                    Style="{DynamicResource {x:Static themes:ResourceKeys.InlineRemoveButtonStyleKey}}"
                                    Margin="4,0,0,0" FontSize="{DynamicResource FontSize.Small}"
                                    ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.RemoveFromOutfit}}" />
                          </Grid>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <StackPanel Orientation="Horizontal" Margin="26,4,0,0"
                                Visibility="{Binding DataContext.HasLeveledListDrafts, RelativeSource={RelativeSource AncestorType=views:OutfitCreatorView}, Converter={StaticResource BoolToVisConverter}}">
                      <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.AddListLabel}}"
                                 VerticalAlignment="Center" Margin="0,0,6,0"
                                 FontSize="{DynamicResource FontSize.Small}"
                                 Foreground="{DynamicResource Brush.TextSecondary}" />
                      <ComboBox Width="200" DisplayMemberPath="EditorId"
                                SelectionChanged="LeveledListPicker_OnSelectionChanged"
                                ItemsSource="{Binding DataContext.LeveledListDrafts, RelativeSource={RelativeSource AncestorType=views:OutfitCreatorView}}"
                                ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.AddListTooltip}}" />
                    </StackPanel>
                  </StackPanel>
                </StackPanel>
              </Border>
            </Grid>

            <Border x:Name="DropIndicatorBottom" Grid.Row="2" Height="2" Background="{DynamicResource Brush.Info}"
                    Margin="0,-4,0,6" Visibility="Collapsed" />
          </Grid>
        </DataTemplate>
      </views:OutfitQueueItemTemplateSelector.DraftTemplate>
      <views:OutfitQueueItemTemplateSelector.SeparatorTemplate>
        <DataTemplate DataType="{x:Type vm:OutfitSeparatorViewModel}">
          <Grid Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisConverter}}"
                AllowDrop="True" Margin="0,8,0,0"
                DragEnter="QueueItem_OnDragEnter"
                DragLeave="QueueItem_OnDragLeave"
                DragOver="QueueItem_OnDragOver"
                Drop="QueueItem_OnDrop">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border x:Name="DropIndicatorTop" Grid.Row="0" Height="2" Background="{DynamicResource Brush.Info}"
                    Margin="0,0,0,2" Visibility="Collapsed" />

            <Border x:Name="SeparatorBorder" Grid.Row="1" Padding="2,6,2,4" BorderThickness="0,0,0,1"
                    BorderBrush="{DynamicResource Brush.Border}"
                    Background="Transparent"
                    Margin="0,0,0,6">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="⠿" FontSize="{DynamicResource FontSize.Small}" Margin="0"
                           Foreground="{DynamicResource Brush.TextDisabled}"
                           VerticalAlignment="Center" Cursor="SizeAll"
                           ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+View.DragToReorder}}"
                           PreviewMouseLeftButtonDown="DragHandle_PreviewMouseLeftButtonDown"
                           PreviewMouseMove="DragHandle_PreviewMouseMove"
                           PreviewMouseLeftButtonUp="DragHandle_PreviewMouseLeftButtonUp" />

                <ToggleButton Grid.Column="1"
                              IsChecked="{Binding IsExpanded}"
                              Margin="0"
                              Background="Transparent"
                              BorderThickness="0"
                              Cursor="Hand"
                              VerticalAlignment="Center">
                  <ToggleButton.Style>
                    <Style TargetType="ToggleButton">
                      <Setter Property="Template">
                        <Setter.Value>
                          <ControlTemplate TargetType="ToggleButton">
                            <TextBlock x:Name="Arrow" Text="▶" FontSize="{DynamicResource FontSize.Small}"
                                       Foreground="{DynamicResource Brush.TextDisabled}" />
                            <ControlTemplate.Triggers>
                              <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Arrow" Property="Text" Value="▼" />
                              </Trigger>
                              <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Arrow" Property="Foreground"
                                        Value="{DynamicResource Brush.TextSecondary}" />
                              </Trigger>
                            </ControlTemplate.Triggers>
                          </ControlTemplate>
                        </Setter.Value>
                      </Setter>
                    </Style>
                  </ToggleButton.Style>
                </ToggleButton>

                <Image Grid.Column="2" Source="{Binding IconUri}" Width="14" Height="14"
                       RenderOptions.BitmapScalingMode="NearestNeighbor"
                       Cursor="Hand" Opacity="0.8" Margin="4,0,2,0"
                       VerticalAlignment="Center"
                       MouseLeftButtonDown="SeparatorIcon_MouseLeftButtonDown"
                       ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+View.ClickChangeIcon}}" />

                <Grid Grid.Column="3" Margin="2,0,0,0" Background="Transparent"
                      MouseLeftButtonDown="SeparatorName_MouseLeftButtonDown"
                      Cursor="Hand" ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+View.ClickRename}}">
                  <TextBlock x:Name="SeparatorNameDisplay"
                             Text="{Binding Name}"
                             FontSize="{DynamicResource FontSize.Small}" FontWeight="SemiBold"
                             Foreground="{DynamicResource Brush.TextSecondary}"
                             VerticalAlignment="Center" />
                  <TextBox x:Name="SeparatorNameEditor"
                           Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                           FontSize="{DynamicResource FontSize.Small}" FontWeight="SemiBold"
                           Foreground="{DynamicResource Brush.TextSecondary}"
                           BorderThickness="0"
                           Background="Transparent"
                           Padding="0"
                           VerticalAlignment="Center"
                           Visibility="Collapsed"
                           LostFocus="SeparatorNameEditor_LostFocus"
                           KeyDown="SeparatorNameEditor_KeyDown" />
                </Grid>

                <TextBlock Grid.Column="4"
                           Text="{Binding ChildCount, StringFormat='({0})'}"
                           FontSize="{DynamicResource FontSize.Small}"
                           Foreground="{DynamicResource Brush.TextDisabled}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Margin="0,0,6,0" />

                <Button Grid.Column="5"
                        Command="{Binding RemoveSelfCommand}"
                        Style="{DynamicResource {x:Static themes:ResourceKeys.InlineRemoveButtonStyleKey}}"
                        Width="20" Height="20" FontSize="{DynamicResource FontSize.Small}"
                        ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+View.RemoveSeparator}}" />
              </Grid>
            </Border>

            <Border x:Name="DropIndicatorBottom" Grid.Row="2" Height="2" Background="{DynamicResource Brush.Info}"
                    Margin="0,-4,0,6" Visibility="Collapsed" />
          </Grid>
        </DataTemplate>
      </views:OutfitQueueItemTemplateSelector.SeparatorTemplate>
    </views:OutfitQueueItemTemplateSelector>
  </UserControl.Resources>
  <Grid Margin="4,0,0,6">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Three-panel layout: Plugins | Armors | Outfit Drafts -->
    <Grid Grid.Row="0" Margin="0,6,0,6">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="220" MinWidth="150" />
        <ColumnDefinition Width="6" />
        <ColumnDefinition Width="2*" MinWidth="300" />
        <ColumnDefinition Width="6" />
        <ColumnDefinition Width="*" MinWidth="280" />
      </Grid.ColumnDefinitions>

      <!-- Left Panel: Plugin Selection -->
      <GroupBox Grid.Column="0" Header="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.SourcePlugins}}"
                Padding="6" BorderThickness="0">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>

          <Grid Grid.Row="0" Margin="0,0,0,6">
            <TextBox x:Name="PluginSearchBox"
                     Text="{Binding OutfitPluginSearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.FilterPlugins}}" />
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+Labels.FilterPlaceholder}}" Foreground="{DynamicResource Brush.TextDisabled}"
                       IsHitTestVisible="False"
                       Margin="6,0,0,0" VerticalAlignment="Center"
                       Visibility="{Binding Text, ElementName=PluginSearchBox, Converter={StaticResource StringEmptyToVisConverter}, ConverterParameter=Inverse}" />
          </Grid>

          <ListBox Grid.Row="1"
                   ItemsSource="{Binding FilteredOutfitPlugins}"
                   SelectedItem="{Binding SelectedOutfitPlugin}"
                   SelectionMode="Single"
                   ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis" ToolTip="{Binding}" />
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </Grid>
      </GroupBox>

      <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="Transparent"
                    utilities:GridSplitterBehavior.PersistKey="OutfitCreator.PluginsColumn" />

      <!-- Middle Panel: Armors from Selected Plugin -->
      <GroupBox Grid.Column="2" Header="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.AvailableArmors}}"
                Padding="6" BorderThickness="0">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Search Bar Row -->
            <Grid Grid.Row="0" Margin="0,0,0,4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="{lex:Loc Key={x:Static res:LocalizationKeys+Labels.Search}}"
                         Style="{DynamicResource {x:Static themes:ResourceKeys.LabelTextStyleKey}}" />
              <TextBox Grid.Column="1" Text="{Binding OutfitSearchText, UpdateSourceTrigger=PropertyChanged}" />
              <TextBlock Grid.Column="2"
                         Margin="8,0,0,0" VerticalAlignment="Center">
                <TextBlock.Text>
                  <MultiBinding Converter="{StaticResource FormatConverter}" ConverterParameter="ArmorPatch_ArmorsFormat">
                  <Binding Path="OutfitArmorsTotalCount" />
                </MultiBinding>
                </TextBlock.Text>
              </TextBlock>
            </Grid>

            <!-- Filter Row -->
            <WrapPanel Grid.Row="1" Orientation="Horizontal">
              <StackPanel Orientation="Horizontal" Margin="0,2,16,2">
                <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.TypeLabel}}"
                           Style="{DynamicResource {x:Static themes:ResourceKeys.LabelTextStyleKey}}" />
                <ComboBox ItemsSource="{Binding OutfitArmorTypeFilterOptions}"
                          SelectedItem="{Binding SelectedOutfitArmorType}"
                          MinWidth="100" />
              </StackPanel>

              <StackPanel Orientation="Horizontal" Margin="0,2,16,2">
                <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+View.SlotLabel}}"
                           Style="{DynamicResource {x:Static themes:ResourceKeys.LabelTextStyleKey}}" />
                <ComboBox ItemsSource="{Binding OutfitSlotFilterOptions}"
                          SelectedItem="{Binding SelectedOutfitSlot}"
                          MinWidth="110" />
              </StackPanel>

              <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+Buttons.ClearFilters}}"
                      Click="ClearOutfitFilters_Click"
                      Padding="8,2" VerticalAlignment="Center" Margin="0,2,0,2" />
            </WrapPanel>
          </Grid>

          <DataGrid x:Name="OutfitArmorsGrid"
                    Grid.Row="1"
                    ItemsSource="{Binding FilteredOutfitArmors}"
                    SelectionChanged="OutfitArmorsGrid_SelectionChanged"
                    MouseDoubleClick="OutfitArmorsGrid_MouseDoubleClick"
                    PreviewMouseLeftButtonDown="OutfitArmorsGrid_PreviewMouseLeftButtonDown"
                    MouseMove="OutfitArmorsGrid_MouseMove"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    IsReadOnly="True"
                    SelectionMode="Extended"
                    CanUserSortColumns="True"
                    RowHeight="40">
            <DataGrid.Columns>
              <DataGridTemplateColumn Header="" Width="Auto" CanUserSort="False">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <Button Content="👁" Click="PreviewArmorButton_Click"
                            Style="{DynamicResource {x:Static themes:ResourceKeys.IconButtonStyleKey}}"
                            Width="28" Height="28" FontSize="{DynamicResource FontSize.Small}"
                            ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.PreviewArmor}}" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.Name}}" Width="2*"
                                  IsReadOnly="True" Binding="{Binding DisplayName, Mode=OneWay}" />
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.EditorID}}" Width="2*"
                                  IsReadOnly="True" Binding="{Binding EditorID, Mode=OneWay}" />
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.Type}}" Width="1*"
                                  IsReadOnly="True" Binding="{Binding ArmorType, Mode=OneWay}" />
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.Slots}}" Width="1.5*"
                                  IsReadOnly="True" Binding="{Binding SlotSummary, Mode=OneWay}" />
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.Mod}}" Width="1.5*"
                                  IsReadOnly="True" Binding="{Binding ModDisplayName, Mode=OneWay}" />
              <DataGridTextColumn Header="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.FormID}}" Width="1*"
                                  IsReadOnly="True" SortMemberPath="FormIdSortable"
                                  Binding="{Binding FormIdDisplay, Mode=OneWay}" />
            </DataGrid.Columns>
          </DataGrid>

          <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
            <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.CreateLeveledList}}"
                    Command="{Binding CreateLeveledListCommand}"
                    Margin="0,0,8,0"
                    Padding="12,6" />
            <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.CreateNewOutfit}}"
                    Command="{Binding CreateOutfitCommand}"
                    Padding="12,6" />
          </StackPanel>
        </Grid>
      </GroupBox>

      <GridSplitter Grid.Column="3" Width="4" HorizontalAlignment="Stretch" Background="Transparent"
                    utilities:GridSplitterBehavior.PersistKey="OutfitCreator.ArmorsColumn" />

      <!-- Right Panel: Outfit Drafts -->
      <GroupBox Grid.Column="4" Header="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.OutfitQueue}}"
                Padding="6" BorderThickness="0"
                IsEnabled="{Binding IsCreatingOutfits, Converter={StaticResource InverseBoolConverter}}">
        <TabControl>
        <TabItem>
        <TabItem.Header>
          <TextBlock>
            <Run Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.OutfitsTab}}" />
            <Run Text="{Binding OutfitDraftCount, StringFormat=' ({0})'}" />
          </TextBlock>
        </TabItem.Header>
        <Grid Margin="0,6,0,0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>

          <!-- Search -->
          <Grid Grid.Row="0" Margin="0,0,0,6">
            <TextBox x:Name="DraftSearchBox"
                     Text="{Binding OutfitDraftSearchText, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+Labels.FilterPlaceholder}}" Foreground="{DynamicResource Brush.TextDisabled}"
                       IsHitTestVisible="False"
                       Margin="6,0,0,0" VerticalAlignment="Center"
                       Visibility="{Binding Text, ElementName=DraftSearchBox, Converter={StaticResource StringEmptyToVisConverter}, ConverterParameter=Inverse}" />
          </Grid>

          <Border Grid.Row="2" Style="{DynamicResource {x:Static themes:ResourceKeys.DropZoneStyleKey}}"
                  Margin="0,0,0,6"
                  AllowDrop="True"
                  DragEnter="NewOutfitDropZone_OnDragEnter"
                  DragLeave="NewOutfitDropZone_OnDragLeave"
                  DragOver="NewOutfitDropZone_OnDragOver"
                  Drop="NewOutfitDropZone_OnDrop">
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.DropArmorsHere}}"
                       Style="{DynamicResource {x:Static themes:ResourceKeys.EmptyStateTextStyleKey}}"
                       TextWrapping="Wrap" />
          </Border>

          <Border Grid.Row="1" Style="{DynamicResource {x:Static themes:ResourceKeys.InfoPanelStyleKey}}"
                  Margin="0,0,0,6"
                  Visibility="{Binding HasExistingPluginOutfits, Converter={StaticResource BoolToVisConverter}}">
            <StackPanel>
              <TextBlock
                FontWeight="Bold" Margin="0,0,0,6">
                <TextBlock.Text>
                  <MultiBinding Converter="{StaticResource FormatConverter}" ConverterParameter="OutfitCreator_ExistingOutfitsFormat">
                  <Binding Path="ExistingOutfits.Count" />
                </MultiBinding>
                </TextBlock.Text>
              </TextBlock>
              <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ExistingOutfits}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <TextBlock Text="{Binding Summary}" Foreground="{DynamicResource Brush.TextDisabled}" Margin="0,0,0,2" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </ScrollViewer>
              <Button Content="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.CopyExistingOutfits}}"
                      Command="{Binding CopyExistingOutfitsCommand}"
                      HorizontalAlignment="Right" Margin="0,6,0,0" Padding="10,4" />
            </StackPanel>
          </Border>

          <Grid Grid.Row="3">
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.NoOutfitsQueued}}"
                       Style="{DynamicResource {x:Static themes:ResourceKeys.EmptyStateTextStyleKey}}"
                       Visibility="{Binding HasOutfitDrafts, Converter={StaticResource InverseBoolToVisConverter}}" />

            <ScrollViewer VerticalScrollBarVisibility="Auto">
              <ItemsControl ItemsSource="{Binding OutfitDrafts}"
                            ItemTemplateSelector="{StaticResource QueueItemTemplateSelector}" />
            </ScrollViewer>
          </Grid>
        </Grid>
        </TabItem>

        <TabItem>
        <TabItem.Header>
          <TextBlock>
            <Run Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.LeveledListsTab}}" />
            <Run Text="{Binding LeveledListDraftCount, StringFormat=' ({0})'}" />
          </TextBlock>
        </TabItem.Header>
          <Grid Margin="0,6,0,0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
              <Border Style="{DynamicResource {x:Static themes:ResourceKeys.InfoPanelStyleKey}}" Margin="0,0,0,6">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <TextBlock Grid.Column="0" Text="&#x2139;" Margin="0,0,8,0" VerticalAlignment="Top"
                             FontWeight="Bold" Foreground="{DynamicResource Brush.Info}" />
                  <TextBlock Grid.Column="1" TextWrapping="Wrap"
                             Foreground="{DynamicResource Brush.TextSecondary}"
                             FontSize="{DynamicResource FontSize.Small}"
                             Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.DistributionHint}}" />
                </Grid>
              </Border>
              <TextBlock Margin="0,0,0,6" TextWrapping="Wrap"
                         Foreground="{DynamicResource Brush.TextSecondary}"
                         FontSize="{DynamicResource FontSize.Small}"
                         Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.CreateHint}}" />
              <Border Style="{DynamicResource {x:Static themes:ResourceKeys.DropZoneStyleKey}}"
                      Margin="0,0,0,6"
                      AllowDrop="True"
                      DragEnter="NewLeveledListDropZone_OnDragEnter"
                      DragLeave="NewLeveledListDropZone_OnDragLeave"
                      DragOver="NewLeveledListDropZone_OnDragOver"
                      Drop="NewLeveledListDropZone_OnDrop">
                <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.DropArmorsHere}}"
                           Style="{DynamicResource {x:Static themes:ResourceKeys.EmptyStateTextStyleKey}}"
                           TextWrapping="Wrap" />
              </Border>
            </StackPanel>

            <Grid Grid.Row="1">
              <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.NoListsQueued}}"
                         Style="{DynamicResource {x:Static themes:ResourceKeys.EmptyStateTextStyleKey}}"
                         Visibility="{Binding HasLeveledListDrafts, Converter={StaticResource InverseBoolToVisConverter}}" />

              <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding LeveledListDrafts}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:LeveledListDraftViewModel}">
                      <Border Padding="6" Margin="0,0,0,6" BorderBrush="{DynamicResource Brush.Border}"
                              BorderThickness="1" CornerRadius="3" Opacity="0.85"
                              AllowDrop="True"
                              DragEnter="LeveledListItem_OnDragEnter"
                              DragOver="LeveledListItem_OnDragOver"
                              DragLeave="LeveledListItem_OnDragLeave"
                              Drop="LeveledListItem_OnDrop">
                        <StackPanel>
                          <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="Auto" />
                              <ColumnDefinition Width="*" />
                              <ColumnDefinition Width="Auto" />
                              <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ToggleButton Grid.Column="0"
                                          IsChecked="{Binding IsExpanded}"
                                          Width="20" Height="20" Margin="0,0,6,0"
                                          VerticalAlignment="Top"
                                          Background="Transparent" BorderThickness="0" Cursor="Hand">
                              <ToggleButton.Style>
                                <Style TargetType="ToggleButton">
                                  <Setter Property="Template">
                                    <Setter.Value>
                                      <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}">
                                          <TextBlock x:Name="Arrow" Text="▶" FontSize="{DynamicResource FontSize.Small}"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"
                                                     Foreground="{DynamicResource Brush.TextSecondary}" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                          <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Arrow" Property="Text" Value="▼" />
                                          </Trigger>
                                        </ControlTemplate.Triggers>
                                      </ControlTemplate>
                                    </Setter.Value>
                                  </Setter>
                                </Style>
                              </ToggleButton.Style>
                            </ToggleButton>

                            <TextBox Grid.Column="1"
                                     Text="{Binding EditorId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                     MaxLength="80" VerticalAlignment="Center"
                                     FontFamily="{DynamicResource Font.Mono}" />

                            <CheckBox Grid.Column="2" Content="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.UseAll}}"
                                      IsChecked="{Binding UseAll}"
                                      VerticalAlignment="Center" Margin="8,0,4,0"
                                      ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.UseAllTooltip}}" />

                            <Button Grid.Column="3"
                                    Command="{Binding RemoveSelfCommand}"
                                    Style="{DynamicResource {x:Static themes:ResourceKeys.InlineRemoveButtonStyleKey}}"
                                    Width="28" Height="28" FontSize="{DynamicResource FontSize.Medium}"
                                    ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.RemoveList}}" />
                          </Grid>

                          <StackPanel Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisConverter}}">
                            <Grid Margin="26,2,0,2">
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                              </Grid.ColumnDefinitions>
                              <TextBlock Grid.Column="0" Text="{lex:Loc Key={x:Static res:LocalizationKeys+Headers.Slots}}"
                                         FontSize="{DynamicResource FontSize.Small}"
                                         Foreground="{DynamicResource Brush.TextDisabled}" />
                              <TextBlock Grid.Column="2" Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.LevelShort}}"
                                         Width="44" Margin="2,0,2,0" TextAlignment="Center"
                                         FontSize="{DynamicResource FontSize.Small}"
                                         Foreground="{DynamicResource Brush.TextDisabled}" />
                              <TextBlock Grid.Column="3" Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.CountHeader}}"
                                         Width="44" Margin="2,0,2,0" TextAlignment="Center"
                                         FontSize="{DynamicResource FontSize.Small}"
                                         Foreground="{DynamicResource Brush.TextDisabled}" />
                              <TextBlock Grid.Column="4" Text="" Width="28" />
                            </Grid>
                            <ItemsControl ItemsSource="{Binding Entries}" Margin="26,0,0,0">
                              <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:LeveledListEntryViewModel}">
                                  <Grid Margin="0,1,0,1">
                                    <Grid.ColumnDefinitions>
                                      <ColumnDefinition Width="70" />
                                      <ColumnDefinition Width="*" />
                                      <ColumnDefinition Width="Auto" />
                                      <ColumnDefinition Width="Auto" />
                                      <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding SlotSummary}"
                                               VerticalAlignment="Center" FontSize="{DynamicResource FontSize.Small}"
                                               TextTrimming="CharacterEllipsis">
                                      <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                          <Setter Property="Foreground" Value="{DynamicResource Brush.TextDisabled}" />
                                          <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConflicting}" Value="True">
                                              <Setter Property="Foreground" Value="{DynamicResource Brush.Warning}" />
                                              <Setter Property="FontWeight" Value="SemiBold" />
                                              <Setter Property="ToolTip"
                                                      Value="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.SlotConflictTooltip}}" />
                                            </DataTrigger>
                                          </Style.Triggers>
                                        </Style>
                                      </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="1" Text="{Binding DisplayName}"
                                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                               FontSize="{DynamicResource FontSize.Small}">
                                      <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                          <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConflicting}" Value="True">
                                              <Setter Property="Foreground" Value="{DynamicResource Brush.Warning}" />
                                            </DataTrigger>
                                          </Style.Triggers>
                                        </Style>
                                      </TextBlock.Style>
                                    </TextBlock>
                                    <TextBox Grid.Column="2" Width="44" Margin="2,0,2,0"
                                             TextAlignment="Center"
                                             Text="{Binding Level, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Grid.Column="3" Width="44" Margin="2,0,2,0"
                                             TextAlignment="Center"
                                             Text="{Binding Count, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Grid.Column="4"
                                            Command="{Binding RemoveCommand}"
                                            Style="{DynamicResource {x:Static themes:ResourceKeys.InlineRemoveButtonStyleKey}}"
                                            Margin="4,0,0,0" FontSize="{DynamicResource FontSize.Small}"
                                            ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.RemoveEntry}}" />
                                  </Grid>
                                </DataTemplate>
                              </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <StackPanel Orientation="Horizontal" Margin="26,4,0,0"
                                        Visibility="{Binding DataContext.HasLeveledListDrafts, RelativeSource={RelativeSource AncestorType=views:OutfitCreatorView}, Converter={StaticResource BoolToVisConverter}}">
                              <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.NestListLabel}}"
                                         VerticalAlignment="Center" Margin="0,0,6,0"
                                         FontSize="{DynamicResource FontSize.Small}"
                                         Foreground="{DynamicResource Brush.TextSecondary}" />
                              <ComboBox Width="200" DisplayMemberPath="EditorId"
                                        SelectionChanged="NestedListPicker_OnSelectionChanged"
                                        ItemsSource="{Binding DataContext.LeveledListDrafts, RelativeSource={RelativeSource AncestorType=views:OutfitCreatorView}}"
                                        ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.NestListTooltip}}" />
                            </StackPanel>
                          </StackPanel>
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </ScrollViewer>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
              <Button Command="{Binding ImportLeveledListsCommand}"
                      Margin="0,0,8,0" Padding="12,8"
                      Content="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.ImportFromPlugin}}"
                      ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.ImportTooltip}}" />
              <Button Command="{Binding SaveLeveledListsCommand}"
                      Padding="16,8"
                      Style="{DynamicResource {x:Static themes:ResourceKeys.PrimaryButtonStyleKey}}"
                      Content="{lex:Loc Key={x:Static res:LocalizationKeys+LeveledList.SaveLeveledLists}}" />
            </StackPanel>
          </Grid>
        </TabItem>
        </TabControl>
      </GroupBox>
    </Grid>

    <Border Grid.Row="1" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,1,0,0"
            Padding="0,8,0,0" Margin="0,0,0,6">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0"
                 VerticalAlignment="Center">
        <TextBlock.Text>
          <MultiBinding Converter="{StaticResource FormatConverter}" ConverterParameter="View_ItemsCountFormat">
                  <Binding Path="OutfitDrafts.Count" />
                </MultiBinding>
        </TextBlock.Text>
      </TextBlock>
      <Button Grid.Column="1" Content="{lex:Loc Key={x:Static res:LocalizationKeys+View.AddSeparator}}" Click="AddSeparator_Click"
              Padding="12,8" Margin="0,0,8,0"
              ToolTip="{lex:Loc Key={x:Static res:LocalizationKeys+View.AddSeparatorTooltip}}" />
      <Button Grid.Column="2" Command="{Binding SaveOutfitsCommand}" Padding="16,8"
              Style="{DynamicResource {x:Static themes:ResourceKeys.PrimaryButtonStyleKey}}"
              IsEnabled="{Binding HasOutfitDrafts}">
        <Grid>
          <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+OutfitCreator.SaveOutfits}}"
                     Visibility="{Binding IsCreatingOutfits, Converter={StaticResource InverseBoolToVisConverter}}" />
          <StackPanel Orientation="Horizontal"
                      Visibility="{Binding IsCreatingOutfits, Converter={StaticResource BoolToVisConverter}}">
            <TextBlock Text="{lex:Loc Key={x:Static res:LocalizationKeys+Status.Saving}}" Margin="0,0,8,0" />
            <ProgressBar IsIndeterminate="True" Width="60" Height="12" />
          </StackPanel>
        </Grid>
      </Button>
    </Grid>
    </Border>
  </Grid>
</UserControl>
